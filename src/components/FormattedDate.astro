---
export interface Props {
	date: Date
}

const { date } = Astro.props
---

<time class='text-sm text-opacity-60 min-w-[90px]' datetime={date.toISOString()}>
	{
		date.toLocaleDateString('en-us', {
			year: 'numeric',
			month: 'short',
			day: 'numeric'
		})
	}
</time>
